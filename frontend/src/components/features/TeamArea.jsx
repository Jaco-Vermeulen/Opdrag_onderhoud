/**
 * Team feature — root for `/team`.
 * Compose slot grid, move controls, and feedback components.
 */
export function TeamArea() {
  return (
    <section>
      <h1>Span</h1>
      <p>Stub — implement team UI from composed components.</p>
    </section>
  );
}
