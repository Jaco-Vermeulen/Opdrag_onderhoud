/**
 * Registration feature — root for `/register`.
 * Compose validation messages and fields as separate components.
 */
export function RegisterArea() {
  return (
    <section>
      <h1>Registreer</h1>
      <p>Stub — implement registration from composed components.</p>
    </section>
  );
}
