/**
 * Backpack feature — root for `/backpack`.
 * Compose list rows, empty state, actions into dedicated components.
 */
export function BackpackArea() {
  return (
    <section>
      <h1>Rugsak</h1>
      <p>Stub — implement backpack UI from composed components.</p>
    </section>
  );
}
