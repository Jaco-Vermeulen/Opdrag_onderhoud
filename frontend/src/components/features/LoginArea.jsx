/**
 * Login feature — root for `/login`.
 * Compose form fields, submit control, and error display components.
 */
export function LoginArea() {
  return (
    <section>
      <h1>Meld aan</h1>
      <p>Stub — implement login from composed components.</p>
    </section>
  );
}
